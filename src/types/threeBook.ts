export interface ThreeBookProps {
  coverLabel?: string;
  accent?: string;
  coverImage?: string;
  onOpen?: () => void;
}

export interface ThreeBookSceneOptions {
  label: string;
  accent: string;
  coverImage?: string;
}
