export type ArrowDirection = 'left' | 'right';

/**
 * Variantes de estilo para las flechas de navegación:
 * - hero:      bg-white/20, p-4, svg 24px (carrusel de tarjetas)
 * - modal:     bg-black/20, p-4, svg 40px (galería fullscreen)
 * - comparison: bg-primary, h-11/w-11, svg 22px (antes/después)
 */
export type ArrowVariant = 'hero' | 'modal' | 'comparison';
